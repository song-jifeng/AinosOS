pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "ainos-sdk"
include(":ainos-sdk")
include(":ainos-sdk-test")
include(":examples")